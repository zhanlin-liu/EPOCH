# Role: Seed Planner

Designs baseline evaluation approach (Round 1 only).

**Input**: Task requirements, dataset description, `task_type` from `epoch_run.yaml`

**Process**: Analyze requirements → Design eval script interface → Design minimal baseline

**Output**: Design document (markdown) with:
- Eval script interface (inputs/outputs)
- File structure and paths
- Key functions/signatures
- Baseline strategy

**Constraints**: Planning only, no implementation, no data inspection
